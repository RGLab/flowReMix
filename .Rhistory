nsamp = 40,
centerCovariance = FALSE,
maxIter = 2, tol = 1e-03))
head(assignmentLsit)
head(assignmentList)
isingfit <- IsingFit::IsingFit(assignmentList, AND = FALSE,
progressbar = FALSE, plot = FALSE)
isingfit
system.time(fit <- subsetResponseMixtureNested(count ~  treatment,
sub.population = factor(data$population),
N = parentcount, id =  ptid,
data = data,
treatment = treatment,
weights = NULL,
rate = 1, updateLag = 3,
nsamp = 40,
centerCovariance = FALSE,
maxIter = 2, tol = 1e-03))
system.time(fit <- subsetResponseMixtureNested(count ~  treatment,
sub.population = factor(data$population),
N = parentcount, id =  ptid,
data = data,
treatment = treatment,
weights = NULL,
rate = 1, updateLag = 3,
nsamp = 40,
centerCovariance = FALSE,
maxIter = 2, tol = 1e-03))
posteriors
head(posteriors)
head(assignmentList)
assignmentList[1:100, ]
system.time(fit <- subsetResponseMixtureNested(count ~  treatment,
sub.population = factor(data$population),
N = parentcount, id =  ptid,
data = data,
treatment = treatment,
weights = NULL,
rate = 1, updateLag = 3,
nsamp = 40,
centerCovariance = FALSE,
maxIter = 2, tol = 1e-03))
library(flowReMix)
system.time(fit <- subsetResponseMixtureNested(count ~  treatment,
sub.population = factor(data$population),
N = parentcount, id =  ptid,
data = data,
treatment = treatment,
weights = NULL,
rate = 1, updateLag = 3,
nsamp = 40,
centerCovariance = FALSE,
maxIter = 2, tol = 1e-03))
library(flowReMix)
system.time(fit <- subsetResponseMixtureNested(count ~  treatment,
sub.population = factor(data$population),
N = parentcount, id =  ptid,
data = data,
treatment = treatment,
weights = NULL,
rate = 1, updateLag = 3,
nsamp = 40,
centerCovariance = FALSE,
maxIter = 2, tol = 1e-03))
system.time(fit <- subsetResponseMixtureNested(count ~  treatment,
sub.population = factor(data$population),
N = parentcount, id =  ptid,
data = data,
treatment = treatment,
weights = NULL,
rate = 1, updateLag = 3,
nsamp = 40,
centerCovariance = FALSE,
maxIter = 2, tol = 1e-03))
library(flowReMix)
system.time(fit <- subsetResponseMixtureNested(count ~  treatment,
sub.population = factor(data$population),
N = parentcount, id =  ptid,
data = data,
treatment = treatment,
weights = NULL,
rate = 1, updateLag = 3,
nsamp = 40,
centerCovariance = FALSE,
maxIter = 2, tol = 1e-03))
library(flowReMix)
library(flowReMix)
system.time(fit <- subsetResponseMixtureRcpp(count ~  treatment,
sub.population = factor(data$population),
N = parentcount, id =  ptid,
data = data,
treatment = treatment,
weights = NULL,
rate = 1, updateLag = 3,
nsamp = 40,
centerCovariance = FALSE,
maxIter = 2, tol = 1e-03))
subjectData
popInd
library(flowReMix)
system.time(fit <- subsetResponseMixtureRcpp(count ~  treatment,
sub.population = factor(data$population),
N = parentcount, id =  ptid,
data = data,
treatment = treatment,
weights = NULL,
rate = 1, updateLag = 3,
nsamp = 40,
centerCovariance = FALSE,
maxIter = 2, tol = 1e-03))
assignment
clusterAssignments[i, ]
which(clusterAssignments[i, ])
which(clusterAssignments[i, ] == 1)
popInd %in% which(clusterAssignments[i, ] == 1)
eta <- subjectData$nullEta
responderSubset <- popInd %in% which(clusterAssignments[i, ] == 1)
eta[responderSubset] <- subjectData$alt[responderSubset]
eta <- subjectData$nullEta
responderSubset <- popInd %in% which(clusterAssignments[i, ] == 1)
eta[responderSubset] <- subjectData$alt[responderSubset]
subjectData$altEta[responderSubset]
clusterAssignment[i,]
clusterAssignments[i, ]
subjectData$altEta
library(flowReMix)
system.time(fit <- subsetResponseMixtureRcpp(count ~  treatment,
sub.population = factor(data$population),
N = parentcount, id =  ptid,
data = data,
treatment = treatment,
weights = NULL,
rate = 1, updateLag = 3,
nsamp = 40,
centerCovariance = FALSE,
maxIter = 2, tol = 1e-03))
library(flowReMix)
system.time(fit <- subsetResponseMixtureRcpp(count ~  treatment,
sub.population = factor(data$population),
N = parentcount, id =  ptid,
data = data,
treatment = treatment,
weights = NULL,
rate = 1, updateLag = 3,
nsamp = 40,
centerCovariance = FALSE,
maxIter = 2, tol = 1e-03))
randomEst <- randomEffectCoordinateMH(as.integer(y), as.integer(N), i, nsamp, nSubsets,
MHcoef, clusterAssignments[i, ], popInd,
eta, estimatedRandomEffects,
condvar, covariance, invcov,
MHattempts, MHsuccess, unifVec)
y
N
i
nsamp
nSubsets
MHcoef
assignment <- as.vector(clusterAssignments[i, ])
assignment <- as.vector(clusterAssignments[i, ])
randomEst <- randomEffectCoordinateMH(as.integer(y), as.integer(N),
i, nsamp, nSubsets,
MHcoef, assignment, popInd,
eta, estimatedRandomEffects,
condvar, covariance, invcov,
MHattempts, MHsuccess, unifVec)
eta
popInd
randomEst <- randomEffectCoordinateMH(as.integer(y), as.integer(N),
i, nsamp, nSubsets,
MHcoef, assignment, as.integer(popInd),
eta, estimatedRandomEffects,
condvar, covariance, invcov,
MHattempts, MHsuccess, unifVec)
estimatedRandomEffects
condvar
as.numeric(condvar)
randomEst <- randomEffectCoordinateMH(as.integer(y), as.integer(N),
i, nsamp, nSubsets,
MHcoef, assignment, as.integer(popInd),
eta, estimatedRandomEffects,
as.numeric(condvar), covariance, invcov,
MHattempts, MHsuccess, unifVec)
covariance
invcov
library(flowReMix)
system.time(fit <- subsetResponseMixtureRcpp(count ~  treatment,
sub.population = factor(data$population),
N = parentcount, id =  ptid,
data = data,
treatment = treatment,
weights = NULL,
rate = 1, updateLag = 3,
nsamp = 40,
centerCovariance = FALSE,
maxIter = 2, tol = 1e-03))
randomEst <- randomEffectCoordinateMH(as.integer(y), as.integer(N),
i, nsamp, nSubsets,
MHcoef, assignment, as.integer(popInd),
eta, estimatedRandomEffects,
as.numeric(condvar), covariance, invcov,
MHattempts, MHsuccess, unifVec)
MHattempts
MHsuccess
unifVec
as.integer(y)
N
as.integer(N)
i
nsamp
nSubsets
eta
randomEst <- randomEffectCoordinateMH(as.integer(y), as.integer(N),
i, nsamp, nSubsets,
MHcoef, assignment, as.integer(popInd),
eta, estimatedRandomEffects,
as.numeric(condvar), covariance, invcov,
MHattempts, MHsuccess, unifVec)
randomEst <- randomEffectCoordinateMH(as.integer(y), as.integer(N),
i, nsamp, nSubsets,
MHcoef, as.integer(assignment), as.integer(popInd),
eta, estimatedRandomEffects,
as.numeric(condvar), covariance, invcov,
MHattempts, MHsuccess, unifVec)
estimatedRandomEffects
eta
library(flowReMix)
system.time(fit <- subsetResponseMixtureRcpp(count ~  treatment,
sub.population = factor(data$population),
N = parentcount, id =  ptid,
data = data,
treatment = treatment,
weights = NULL,
rate = 1, updateLag = 3,
nsamp = 40,
centerCovariance = FALSE,
maxIter = 2, tol = 1e-03))
randomEst <- randomEffectCoordinateMH(as.integer(y), as.integer(N),
i, nsamp, nSubsets,
MHcoef, as.integer(assignment), as.integer(popInd),
eta, estimatedRandomEffects,
as.numeric(condvar), covariance, invcov,
MHattempts, MHsuccess, unifVec)
library(flowReMix)
system.time(fit <- subsetResponseMixtureRcpp(count ~  treatment,
sub.population = factor(data$population),
N = parentcount, id =  ptid,
data = data,
treatment = treatment,
weights = NULL,
rate = 1, updateLag = 3,
nsamp = 40,
centerCovariance = FALSE,
maxIter = 2, tol = 1e-03))
randomEst <- randomEffectCoordinateMH(as.integer(y), as.integer(N),
i, nsamp, nSubsets,
MHcoef, as.integer(assignment), as.integer(popInd),
eta, estimatedRandomEffects,
as.numeric(condvar), covariance, invcov,
MHattempts, MHsuccess, unifVec)
condvar
as.numeric(condvar)
covariance
invcov
MHattempts
randomEst <- randomEffectCoordinateMH(as.integer(y), as.integer(N),
i, nsamp, nSubsets,
MHcoef, as.integer(assignment), as.integer(popInd),
eta, estimatedRandomEffects,
as.numeric(condvar), covariance, invcov,
as.numeric(MHattempts), as.numeric(MHsuccess), unifVec)
randomEst <- randomEffectCoordinateMH(as.integer(y), as.integer(N),
as.integer(i), nsamp, nSubsets,
MHcoef,
as.vector(assignment),
as.integer(popInd),
as.numeric(eta),
as.matrix(estimatedRandomEffects),
as.numeric(condvar),
covariance,
invcov,
MHattempts, MHsuccess,
unifVec)
randomEst
library(flowReMix)
library(flowReMix)
system.time(fit <- subsetResponseMixtureRcpp(count ~  treatment,
sub.population = factor(data$population),
N = parentcount, id =  ptid,
data = data,
treatment = treatment,
weights = NULL,
rate = 1, updateLag = 3,
nsamp = 40,
centerCovariance = FALSE,
maxIter = 2, tol = 1e-03))
system.time(fit <- subsetResponseMixtureRcpp(count ~  treatment,
sub.population = factor(data$population),
N = parentcount, id =  ptid,
data = data,
treatment = treatment,
weights = NULL,
rate = 1, updateLag = 3,
nsamp = 40,
centerCovariance = FALSE,
maxIter = 2, tol = 1e-03))
randomEst <- randomEffectCoordinateMH(as.integer(y), as.integer(N),
as.integer(i), nsamp, nSubsets,
MHcoef,
as.vector(assignment),
as.integer(popInd),
as.numeric(eta),
randomEst,
as.numeric(condvar), covariance, invcov,
MHattempts, MHsuccess,
unifVec)
randomEst
MHsuccess
MHattempts
library(flowReMix)
randomEst <- randomEffectCoordinateMH(as.integer(y), as.integer(N),
as.integer(i), nsamp, nSubsets,
MHcoef,
as.vector(assignment),
as.integer(popInd),
as.numeric(eta),
randomEst,
as.numeric(condvar), covariance, invcov,
MHattempts, MHsuccess,
unifVec)
system.time(fit <- subsetResponseMixtureRcpp(count ~  treatment,
sub.population = factor(data$population),
N = parentcount, id =  ptid,
data = data,
treatment = treatment,
weights = NULL,
rate = 1, updateLag = 3,
nsamp = 40,
centerCovariance = FALSE,
maxIter = 2, tol = 1e-03))
MHattempts
MHsuccess
randomEst <- randomEffectCoordinateMH(as.integer(y), as.integer(N),
as.integer(i), nsamp, nSubsets,
MHcoef,
as.vector(assignment),
as.integer(popInd),
as.numeric(eta),
randomEst,
as.numeric(condvar), covariance, invcov,
MHattempts, MHsuccess,
unifVec)
MHattempts
MHsuccess
randomEst
randomEst <- randomEffectCoordinateMH(as.integer(y), as.integer(N),
as.integer(i), nsamp, nSubsets,
MHcoef,
as.vector(assignment),
as.integer(popInd),
as.numeric(eta),
randomEst,
as.numeric(condvar), covariance, invcov,
MHattempts, MHsuccess,
unifVec)
randomEst
library(flowReMix)
system.time(fit <- subsetResponseMixtureRcpp(count ~  treatment,
sub.population = factor(data$population),
N = parentcount, id =  ptid,
data = data,
treatment = treatment,
weights = NULL,
rate = 1, updateLag = 3,
nsamp = 40,
centerCovariance = FALSE,
maxIter = 2, tol = 1e-03))
randomEst
randomEst <- randomEffectCoordinateMH(as.integer(y), as.integer(N),
as.integer(i), nsamp, nSubsets,
MHcoef,
as.vector(assignment),
as.integer(popInd),
as.numeric(eta),
randomEst,
as.numeric(condvar), covariance, invcov,
MHattempts, MHsuccess,
unifVec)
randomEst
MHattempts
MHsuccess
library(flowReMix)
library(flowReMix)
system.time(fit <- subsetResponseMixtureRcpp(count ~  treatment,
sub.population = factor(data$population),
N = parentcount, id =  ptid,
data = data,
treatment = treatment,
weights = NULL,
rate = 1, updateLag = 3,
nsamp = 40,
centerCovariance = FALSE,
maxIter = 2, tol = 1e-03))
randomEst
randomEst <- randomEffectCoordinateMH(as.integer(y), as.integer(N),
as.integer(i), nsamp, nSubsets,
MHcoef,
as.vector(assignment),
as.integer(popInd),
as.numeric(eta),
randomEst,
as.numeric(condvar), covariance, invcov,
MHattempts, MHsuccess,
unifVec)
randomEst
MHsuccess
library(flowReMix)
randomEst <- randomEffectCoordinateMH(as.integer(y), as.integer(N),
as.integer(i), nsamp, nSubsets,
MHcoef,
as.vector(assignment),
as.integer(popInd),
as.numeric(eta),
randomEst,
as.numeric(condvar), covariance, invcov,
MHattempts, MHsuccess,
unifVec)
system.time(fit <- subsetResponseMixtureRcpp(count ~  treatment,
sub.population = factor(data$population),
N = parentcount, id =  ptid,
data = data,
treatment = treatment,
weights = NULL,
rate = 1, updateLag = 3,
nsamp = 40,
centerCovariance = FALSE,
maxIter = 2, tol = 1e-03))
randomEst <- randomEffectCoordinateMH(as.integer(y), as.integer(N),
as.integer(i), nsamp, nSubsets,
MHcoef,
as.vector(assignment),
as.integer(popInd),
as.numeric(eta),
randomEst,
as.numeric(condvar), covariance, invcov,
MHattempts, MHsuccess,
unifVec)
randomEst
library(flowReMix)
library(flowReMix)
randomEst <- randomEffectCoordinateMH(as.integer(y), as.integer(N),
as.integer(i), nsamp, nSubsets,
MHcoef,
as.vector(assignment),
as.integer(popInd),
as.numeric(eta),
randomEst,
as.numeric(condvar), covariance, invcov,
MHattempts, MHsuccess,
unifVec)
system.time(fit <- subsetResponseMixtureRcpp(count ~  treatment,
sub.population = factor(data$population),
N = parentcount, id =  ptid,
data = data,
treatment = treatment,
weights = NULL,
rate = 1, updateLag = 3,
nsamp = 40,
centerCovariance = FALSE,
maxIter = 2, tol = 1e-03))
randomEst <- randomEffectCoordinateMH(as.integer(y), as.integer(N),
as.integer(i), nsamp, nSubsets,
MHcoef,
as.vector(assignment),
as.integer(popInd),
as.numeric(eta),
randomEst,
as.numeric(condvar), covariance, invcov,
MHattempts, MHsuccess,
unifVec)
library(flowReMix)
system.time(fit <- subsetResponseMixtureRcpp(count ~  treatment,
sub.population = factor(data$population),
N = parentcount, id =  ptid,
data = data,
treatment = treatment,
weights = NULL,
rate = 1, updateLag = 3,
nsamp = 40,
centerCovariance = FALSE,
maxIter = 2, tol = 1e-03))
randomEst <- randomEffectCoordinateMH(as.integer(y), as.integer(N),
as.integer(i), nsamp, nSubsets,
MHcoef,
as.vector(assignment),
as.integer(popInd),
as.numeric(eta),
randomEst,
as.numeric(condvar), covariance, invcov,
MHattempts, MHsuccess,
unifVec)
system.time(fit <- subsetResponseMixtureRcpp(count ~  treatment,
sub.population = factor(data$population),
N = parentcount, id =  ptid,
data = data,
treatment = treatment,
weights = NULL,
rate = 1, updateLag = 3,
nsamp = 40,
centerCovariance = FALSE,
maxIter = 2, tol = 1e-03))
library(flowReMix)
